name: slide_escape
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # UI Icons
  cupertino_icons: ^1.0.8

  # Map feature you already use
  game_levels_scrolling_map: ^0.0.7

  # 🧩 State management for game logic
  flutter_riverpod: ^2.5.1

  # 🧠 Local storage for progress, stars, unlocked levels
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    # ✅ Existing map and UI assets
    - assets/map_background/background.jpg
    - assets/level_background/normal_level_background.png
    - assets/app_bar/frames/frame1.png
    - assets/app_bar/frames/frame2.png
    - assets/app_bar/frames/frame3.png
    - assets/app_bar/frames/frame4.png
    - assets/app_bar/goldbar.png
    - assets/app_bar/level_background.png
    - assets/app_bar/settings.png
    - assets/app_footer/leaderboard.png
    - assets/app_footer/map.png
    - assets/app_footer/shop.png
    - assets/app_bar/profile_pictures/star.png

    # ✅ Add game-level JSONs for Unblock Me board
    - assets/levels/

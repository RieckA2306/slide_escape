name: slide_escape
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  game_levels_scrolling_map: ^0.0.6   # map added

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:         # ✅ register the Map assests
    - assets/drawable/map_vertical.png
    - assets/svg/map_vertical.svg
